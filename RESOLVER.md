# Resolver

What I'd actually type → which skill should take it.

| I type | Skill |
|---|---|
| "give me a forum brief" | `copa-forum-brief` |
| "what should I reply to on COPA?" | `copa-forum-brief` |
| "anything worth jumping in on at the forums" | `copa-forum-brief` |
| "find me threads" | `copa-forum-brief` |
| "where should I be visible on the forums" | `copa-forum-brief` |
| "post this to the group" | `post-to-google-group` |
| "send this to SJPA Announcements" | `post-to-google-group` |
| "queue up a post about the runway closure" | `post-to-google-group` |
| *(forwards a NOTAM email)* "members should know about this" | `post-to-google-group` |
| "file this" | `file-it` |
| "put this where it belongs" | `file-it` |
| "where should this go?" | `file-it` |
| "clean up my Downloads folder" | `file-it` |
| "organize these scans" | `file-it` |
| "morning brief" | `morning` |
| "run my morning brief" | `morning` |
| "set up the morning brief for weekdays" | `morning` |
| "/morning" | `morning` |
| "explain my usage" | `explain-usage` |
| "where did my tokens go?" | `explain-usage` |
| "why was that session so expensive" | `explain-usage` |
| "write me a haiku" | `weather-haiku` |
| "haiku about the weather" | `weather-haiku` |
| "which model should I use for COPA Commander?" | `model-radar` ⚠️ |
| "is there anything cheaper than what I'm running?" | `model-radar` ⚠️ |
| "re-check my model choices" | `model-radar` ⚠️ |
| "import my ChatGPT memories" | `import-memory` ⚠️ |
| *(pastes a memory export)* "add these" | `import-memory` ⚠️ |
| "skillify this" | `add-skill` |
| "make this a skill" | `add-skill` |
| "add a skill that…" | `add-skill` |
| "save this as a skill" | `add-skill` |
| "change my file-it skill so…" | `add-skill` |
| "ping the smoke test" | `zz-smoke-test` |

⚠️ = the phrase is real, but the skill's own `description:` doesn't contain
anything like it, so the router won't match. See the audit below.
